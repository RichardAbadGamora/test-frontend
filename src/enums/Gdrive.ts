export enum Menu {
  HOME = 'home',
  STARRED = 'starred',
  SHARED = 'shared',
  FILES = 'files',
}

export enum Display {
  LIST = 'list',
  GRID = 'grid'
}

export enum FileMenu {
  SHARE = 'share',
  RENAME = 'rename',
  DETAILS = 'details',
  MOVE = 'move',
  REMOVE = 'remove',
  STAR = 'add-to-starred',
  UNSTAR = 'remove-from-starred',
  COPY_LINK = 'copy-link',
  MANAGE_ACCESS = 'manage_access',
}